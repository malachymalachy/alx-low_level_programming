#include "main.h"

/**
 * _abs - Main function entry point
 * @n : character to check the case
 * Return: Always zero
 */
int _abs(int n) /* The function returns void */
{
	return (n >= 0 ? n : n * -1);
}
